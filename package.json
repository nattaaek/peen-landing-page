{
  "name": "peen-landing-page",
  "private": true,
  "scripts": {
    "start": "python3 -m http.server 8080",
    "build:vendor:three": "esbuild js/vendor-three.js --bundle --format=iife --minify --outfile=js/vendor-three.bundle.js --legal-comments=none",
    "build:landing:js": "esbuild tweaks-panel.jsx primitives.jsx hero-3d.jsx phone-screens.jsx sections.jsx app.jsx --format=iife --minify --outdir=js --entry-names=[name].bundle --jsx=transform --legal-comments=none",
    "build:hero": "esbuild js/legacy/hero-glb.js --bundle --format=iife --minify --outfile=js/legacy/hero-glb.bundle.js --legal-comments=none"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "three": "0.170.0"
  }
}
